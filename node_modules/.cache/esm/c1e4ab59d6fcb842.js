let Router,LoginValidation,Validator,User;_d6f‍.x([["default",()=>_d6f‍.o]]);_d6f‍.w("express",[["Router",["Router"],function(v){Router=v}]]);_d6f‍.w("../validators/index",[["LoginValidation",["LoginValidation"],function(v){LoginValidation=v}]]);_d6f‍.w("../middlewares/validator",[["default",["Validator"],function(v){Validator=v}]]);_d6f‍.w("../models",[["User",["User"],function(v){User=v}]]);




const router = Router();

/**
 * @description Login a user
 * @api /users/api/login
 * @access PUBLIC
 * @type POST
 */
router.post("/api/login", LoginValidation, Validator, async (req, res) => {
  try {
    let { username, password } = req.body;
    // Check if user exist in DB
    let user = await User.findOne({ username });
    if (!user) {
      return res.status(404).json({
        success: false,
        msg: "User not found",
      });
    }
    // Check if password is correct
    if (!(await user.comparePassword(password))) {
      return res.status(401).json({
        success: false,
        msg: "Password is incorrect",
      });
    }
    // If all OK, generate a token
    let token = await user.generateJWT();

    return res.status(200).json({
      success: true,
      user: user.getUserInfo(),
      token: `Bearer ${token}`,
    });
  } catch (e) {
    _d6f‍.g.console.log(e);
    return res.status(500).json({
      success: false,
      msg: "An error occured",
    });
  }
});

_d6f‍.d(router);
